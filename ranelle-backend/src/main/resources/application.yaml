ktor:
    application:
        modules:
            - web.ApplicationKt.module
    deployment:
        host: 0.0.0.0gi
        port: 8080

database:
    host: ${?POSTGRES_HOST}
    port: ${?POSTGRES_PORT}
    name: ${?POSTGRES_DB}
    user: ${?POSTGRES_USER}
    password: ${?POSTGRES_PASSWORD}
